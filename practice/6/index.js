/**
 * jsonp不是真正的ajax
 *
 * 浏览器的同源策略（服务器端没有同源策略）和跨域
 *
 * img和script标签能绕过跨域
 * */

/**
 * document load与ready的区别
 *
 * load： 页面的全部资源加载完才会执行，包括图片、视频等  load
 * ready： DOM渲染完即可执行，此时图片、视频可能还没有加载完  DOMContentLoaded
 * */

/**
 * == 和 === 的区别
 * == 会尝试类型转换
 * === 严格相等
 *
 * ==null 相当于 ===null||===undefined
 * */
