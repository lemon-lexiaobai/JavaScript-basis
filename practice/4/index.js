/**
 * call 和 apply 都是为了改变某个函数运行时的上下文而存在的，换句话说就是改变函数体内部this指向
 *
 * 函数call 和 apply的区别
 *
 * 传递的参数不一样
 * call 接受参数列表
 * apply 接受一个包含多个参数的数组
 * 相同点： 返回的都是函数执行结果
 *
 * bind 与 call的区别：
 * bind 返回的是函数，需要调用才会执行
 * 参数与call一致，是参数列表
 * */
